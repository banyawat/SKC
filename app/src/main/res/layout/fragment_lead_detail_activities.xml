<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="36dp"
    tools:context=".LeadModule.LeadDetailActivities">

    <Button
        android:id="@+id/lead_detail_activity_add_button"
        android:layout_width="120dp"
        android:layout_height="48dp"
        android:layout_alignParentRight="true"
        android:text="เพิ่มกิจกรรม" />

    <!--RecyclerView of Wishlist-->
    <TextView
        android:id="@+id/lead_detail_activities_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="COUNT"
        android:layout_below="@id/lead_detail_activity_add_button"
        android:layout_marginTop="12dp"
        android:layout_alignParentRight="true"
        android:layout_gravity="right" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/lead_detail_activities_list"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/lead_detail_activities_count" />

</RelativeLayout>